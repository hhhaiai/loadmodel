# Models Archive

这个目录用于保存 `assets/models/` 的压缩分片。

默认约定：

- 归档名：`assets_models.tar.gz`
- 分片大小：`49MB`
- 恢复命令：`./restore_models.sh`
- 重新打包命令：`./pack_models.sh`

说明：

- 分片文件会保留在仓库中。
- 本地解压后的 `assets/models/` 属于工作副本，不建议直接提交。
